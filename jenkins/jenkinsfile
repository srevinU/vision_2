pipeline {
    agent any
    environment {
        TT = "${BRANCH_NAME == 'staging' ? 'staging' : 'production'}"
    }
    tools {nodejs 'node'}
    stages {
        stage('init') {
            steps {
                echo 'init'
                sh "echo ${BRANCH_NAME}"
                sh "echo ${env.TT}"
                sh "echo ${TT}"
            }
        }
        stage('Build') {
            steps {
                script {
                    def groovy = load "${WORKSPACE}/jenkins/build.groovy"
                    groovy.buildApp()
                }
            }
        }
        stage('Test') {
            steps {
                script {
                    def groovy = load "${WORKSPACE}/jenkins/test.groovy"
                    groovy.testApp()   
                }
            }
        }
        stage('Deploy') {
            steps {
                script {
                    def groovy = load "${WORKSPACE}/jenkins/deploy.groovy"
                    groovy.deployApp()
                }
            }
        }
    }
    post {
        always {
            echo 'This will run always'
        }
        success {
            echo 'This will run on success'
        }
        failure {
            echo 'This will run on failure'
        }
    }
}
