pipeline {
    agent any
    stages {
        stage('init') {
            steps {
                script {
                    echo 'init'
                    // def path = pwd()
                    // def functions = load 'script.groovy'
                    // functions.buildApp()
                }
            }
        }
        stage('Build') {
            steps {
                echo 'build'
            }
        }
        stage('Test') {
            steps {
                echo 'test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'deploy'
            }
        }
    }
}

post {
    always {
    //
    }
    success {
    //
    }
    failure {
    //
    }
}
