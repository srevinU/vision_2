pipeline {
    agent any
    stages {
        stage('init') {
            steps {
                script {
                    // def path = pwd()
                    def functions = load './script.groovy'
                    functions.buildApp()
                }
            }
        }
        stage('Build') {
        }
        stage('Test') {
        }
        stage('Deploy') {
        }
    }
}

post {
    always {
    //
    }
    success {
    //
    }
    failure {
    //
    }
}
